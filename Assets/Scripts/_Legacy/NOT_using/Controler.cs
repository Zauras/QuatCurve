using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Controler : MonoBehaviour {
/*
	public Lib_QuaternionAritmetics H;

	// Use this for initialization
	void Start () {
		H = GetComponent<Lib_QuaternionAritmetics>();
		Quaternion targetQuat = gameObject.transform.rotation;
		print (targetQuat);
		Quaternion rotationVec3 = H.Rotation(targetQuat, 34.0f);
		gameObject.transform.rotation = rotationVec3;
		print (rotationVec3);
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
	 */
}
